<!--index.wxml-->
<view class="main">
<!--
  <view class="scroll-item">
      <view id="title">{{title}}</view>
      <view id="text">{{description}}</view>
      <view>
        <image id="image"
            style="width: 70px; height: 70px; background-color: #eeeeee;"
            mode="scaleToFill"
            src="{{src}}"
          ></image>
      </view>
    </view>
    -->

  <scroll-view class="scroll-view"
    scroll-y
    style="height: 1000px;"
    bindscrolltoupper="upper"
    bindscrolltolower="lower"
    bindscroll="scroll"
    scroll-into-view="{{toView}}"
    scroll-top="{{scrollTop}}"
  >
    <view class="scroll-item">
      <view id="title">{{title}}</view>
      <view id="text">{{description}}</view>
      <view>
        <image id="image"
            style="width: 70px; height: 70px; background-color: #eeeeee;"
            mode="scaleToFill"
            src="{{src}}"
          ></image>
      </view>
    </view>
    <view class="scroll-item">
      <view id="title">{{title}}</view>
      <view id="text">{{description}}</view>
      <view>
        <image id="image"
            style="width: 70px; height: 70px; background-color: #eeeeee;"
            mode="scaleToFill"
            src="{{src}}"
          ></image>
      </view>
    </view>
  </scroll-view>



  
</view>